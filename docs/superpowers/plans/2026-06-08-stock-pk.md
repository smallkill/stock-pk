# 台股多檔 PK 工具 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 一個台股「最多 5 檔單筆投資 PK」工具:選 2~5 檔、日期區間、金額 → 算每檔含息總報酬、vs 基準差額、標贏家 + 多線成長圖;含訪客數;獨立部署於 Cloudflare。

**Architecture:** 前端 Astro(Pages)純前端算與畫;Worker 代理 Yahoo chart API 取 adjclose + 訪客埋點(D1);股票清單 build 時由 FinMind 產 stocks.json 供前端候選。

**Tech Stack:** Cloudflare Workers + D1 + Pages、Astro 5、TypeScript、Vitest(web 用 node 環境測純函式;api 用 @cloudflare/vitest-pool-workers)。資料:Yahoo Finance chart API(adjclose)、FinMind TaiwanStockInfo(清單)。

**Repo:** `~/projects/stock-pk`(已 git init,branch `main`)。**獨立 repo,與 devbox 無關。**

---

## File Structure

```
stock-pk/
  package.json            # root,npm workspaces: ["api","web"]
  tsconfig.base.json
  .gitignore
  README.md
  scripts/build-stocks.mjs # FinMind → web/public/stocks.json
  api/                    # Cloudflare Worker
    src/{index.ts, yahoo.ts, visits.ts}
    test/{yahoo.test.ts, visits.test.ts, routes.test.ts, env.d.ts}
    {wrangler.toml, schema.sql, vitest.config.ts, tsconfig.json, package.json}
  web/                    # Astro on Pages
    src/lib/{dates.ts, autocomplete.ts, compare.ts, chart.ts, api.ts}
    src/components/{StockSlots.astro, Results.astro}
    src/pages/index.astro
    src/layouts/Base.astro
    public/stocks.json
    test/{dates.test.ts, autocomplete.test.ts, compare.test.ts, chart.test.ts}
    {astro.config.mjs, tsconfig.json, package.json, vitest.config.ts}
  .github/workflows/ci.yml
```

---

### Task 1: Repo scaffold(root + workspaces)

**Files:** Create `package.json`、`tsconfig.base.json`、`.gitignore`、`README.md`

- [ ] **Step 1: root package.json**

```json
{
  "name": "stock-pk",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "workspaces": ["api", "web"],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc -p api/tsconfig.json && tsc -p web/tsconfig.json",
    "test": "npm run test --workspaces --if-present",
    "build:stocks": "node scripts/build-stocks.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "eslint": "^9.9.0",
    "typescript-eslint": "^8.2.0"
  }
}
```

- [ ] **Step 2: tsconfig.base.json**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "noUnusedLocals": true,
    "skipLibCheck": true,
    "verbatimModuleSyntax": true,
    "isolatedModules": true
  }
}
```

- [ ] **Step 3: .gitignore**

```
node_modules/
dist/
.wrangler/
.dev.vars
.cf_*
.DS_Store
web/public/stocks.json
```
（stocks.json 由 build 產出;若想隨 repo 附帶可改成不忽略——實作時 Task 2 會產出,先忽略避免大檔進 git 歷史;部署時於 CI build。）

- [ ] **Step 4: README.md**（簡述:台股多檔 PK、技術棧、`npm run build:stocks`、本機開發、部署;附「by Derek Chen」與履歷連結 placeholder）

- [ ] **Step 5: eslint.config.js**(複製一份 typescript-eslint flat config recommended,與 devbox 同):

```js
import tseslint from "typescript-eslint";
export default tseslint.config(
  { ignores: ["**/dist/**", "**/.wrangler/**", "**/node_modules/**"] },
  ...tseslint.configs.recommended,
);
```

- [ ] **Step 6: Commit**
```bash
git add package.json tsconfig.base.json .gitignore README.md eslint.config.js
git commit -m "chore: repo scaffold (workspaces, tsconfig, lint)"
```

---

### Task 2: scripts/build-stocks.mjs(FinMind → stocks.json)

**Files:** Create `scripts/build-stocks.mjs`

> 純資料腳本,無單元測試;以「實跑產出 + 驗證內容」為驗收。

- [ ] **Step 1: 實作**

```js
// 由 FinMind TaiwanStockInfo 產生 web/public/stocks.json:
// [{ code, name, suffix }]  suffix: 'TW'(上市) | 'TWO'(上櫃);興櫃(emerging)排除。
import { writeFile, mkdir } from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";

const here = path.dirname(fileURLToPath(import.meta.url));
const outPath = path.resolve(here, "../web/public/stocks.json");

const res = await fetch("https://api.finmindtrade.com/api/v4/data?dataset=TaiwanStockInfo");
if (!res.ok) throw new Error(`FinMind ${res.status}`);
const json = await res.json();
const rows = json.data ?? [];

const seen = new Set();
const out = [];
for (const r of rows) {
  const market = r.type === "twse" ? "TW" : r.type === "tpex" ? "TWO" : null;
  if (!market) continue; // 排除 emerging(興櫃)
  const code = String(r.stock_id ?? "").trim();
  const name = String(r.stock_name ?? "").trim();
  if (!code || !name || seen.has(code)) continue;
  seen.add(code);
  out.push({ code, name, suffix: market });
}
out.sort((a, b) => a.code.localeCompare(b.code));

await mkdir(path.dirname(outPath), { recursive: true });
await writeFile(outPath, JSON.stringify(out), "utf8");
console.log(`stocks.json: ${out.length} 檔 → ${outPath}`);
```

- [ ] **Step 2: 實跑驗證**

Run: `node scripts/build-stocks.mjs`
Expected: 印出約 3000+ 檔;`web/public/stocks.json` 產生。驗證:
`node -e "const s=require('./web/public/stocks.json');console.log(s.length, s.find(x=>x.code==='0050'), s.find(x=>x.code==='6488'))"`
Expected: 0050 → suffix TW、6488 → suffix TWO。

- [ ] **Step 3: Commit**
```bash
git add scripts/build-stocks.mjs
git commit -m "feat: build-stocks script (FinMind → stocks.json)"
```

---

### Task 3: web workspace 骨架 + vitest

**Files:** Create `web/package.json`、`web/tsconfig.json`、`web/astro.config.mjs`、`web/vitest.config.ts`、`web/src/env.d.ts`

- [ ] **Step 1: web/package.json**

```json
{
  "name": "@stock-pk/web",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "test": "vitest run"
  },
  "dependencies": { "astro": "^5.0.0" },
  "devDependencies": { "vitest": "~3.2.0" }
}
```

- [ ] **Step 2: web/tsconfig.json**

```json
{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro", "src/**/*"],
  "exclude": ["dist"]
}
```

- [ ] **Step 3: web/astro.config.mjs**

```js
import { defineConfig } from "astro/config";
export default defineConfig({ site: "https://derek-stock-pk.pages.dev" });
```

- [ ] **Step 4: web/vitest.config.ts**

```ts
import { defineConfig } from "vitest/config";
export default defineConfig({ test: { environment: "node", include: ["test/**/*.test.ts"] } });
```

- [ ] **Step 5: 裝依賴**

Run: `cd ~/projects/stock-pk && npm install`
Expected: 成功(astro + vitest 裝好)

- [ ] **Step 6: Commit**
```bash
git add web/package.json web/tsconfig.json web/astro.config.mjs web/vitest.config.ts package-lock.json
git commit -m "chore(web): astro + vitest scaffold"
```

---

### Task 4: web/src/lib/dates.ts(日期快捷 + 驗證)

**Files:** Create `web/src/lib/dates.ts`、`web/test/dates.test.ts`

- [ ] **Step 1: 失敗測試** — `web/test/dates.test.ts`

```ts
import { describe, it, expect } from "vitest";
import { presetRange, clampEnd, validRange, toYmd, PRESETS } from "../src/lib/dates";

const NOW = Date.UTC(2026, 5, 8); // 2026-06-08

describe("PRESETS", () => {
  it("含 1m/6m/1y/3y/5y/10y", () => {
    expect(PRESETS.map((p) => p.key)).toEqual(["1m", "6m", "1y", "3y", "5y", "10y"]);
  });
});
describe("presetRange", () => {
  it("1y → 起 = 一年前、訖 = 今天", () => {
    const { from, to } = presetRange("1y", NOW);
    expect(toYmd(to)).toBe("2026-06-08");
    expect(toYmd(from)).toBe("2025-06-08");
  });
  it("1m → 一個月前", () => {
    expect(toYmd(presetRange("1m", NOW).from)).toBe("2026-05-08");
  });
});
describe("clampEnd", () => {
  it("未來日夾到今天", () => {
    expect(clampEnd(Date.UTC(2030, 0, 1), NOW)).toBe(NOW);
  });
  it("過去日不變", () => {
    const past = Date.UTC(2020, 0, 1);
    expect(clampEnd(past, NOW)).toBe(past);
  });
});
describe("validRange", () => {
  it("from < to → ok", () => {
    expect(validRange(Date.UTC(2020,0,1), Date.UTC(2021,0,1)).ok).toBe(true);
  });
  it("from >= to → 錯", () => {
    expect(validRange(Date.UTC(2021,0,1), Date.UTC(2021,0,1)).ok).toBe(false);
  });
});
```

- [ ] **Step 2: 跑 → 失敗** — Run: `cd web && npx vitest run dates` → FAIL

- [ ] **Step 3: 實作** — `web/src/lib/dates.ts`

```ts
export interface Preset { key: string; label: string; months: number; }
export const PRESETS: Preset[] = [
  { key: "1m", label: "1個月", months: 1 },
  { key: "6m", label: "半年", months: 6 },
  { key: "1y", label: "1年", months: 12 },
  { key: "3y", label: "3年", months: 36 },
  { key: "5y", label: "5年", months: 60 },
  { key: "10y", label: "10年", months: 120 },
];

/** ts(毫秒,UTC)→ 'YYYY-MM-DD'。 */
export function toYmd(ts: number): string {
  return new Date(ts).toISOString().slice(0, 10);
}

/** 快捷區間:訖 = now,起 = now 往前推 N 個月。 */
export function presetRange(key: string, now: number): { from: number; to: number } {
  const p = PRESETS.find((x) => x.key === key);
  const months = p ? p.months : 12;
  const d = new Date(now);
  const from = Date.UTC(d.getUTCFullYear(), d.getUTCMonth() - months, d.getUTCDate());
  return { from, to: now };
}

/** 結束日夾到今天(不可選未來)。 */
export function clampEnd(end: number, now: number): number {
  return end > now ? now : end;
}

/** 驗證:from 必須早於 to。 */
export function validRange(from: number, to: number): { ok: boolean; reason?: string } {
  if (from >= to) return { ok: false, reason: "from_after_to" };
  return { ok: true };
}
```

- [ ] **Step 4: 跑 → 通過** — Run: `cd web && npx vitest run dates` → PASS

- [ ] **Step 5: Commit**
```bash
git add web/src/lib/dates.ts web/test/dates.test.ts
git commit -m "feat(web): date presets + range validation (TDD)"
```

---

### Task 5: web/src/lib/autocomplete.ts(候選過濾)

**Files:** Create `web/src/lib/autocomplete.ts`、`web/test/autocomplete.test.ts`

- [ ] **Step 1: 失敗測試**

```ts
import { describe, it, expect } from "vitest";
import { filterStocks, tickerOf, type Stock } from "../src/lib/autocomplete";

const LIST: Stock[] = [
  { code: "2330", name: "台積電", suffix: "TW" },
  { code: "2317", name: "鴻海", suffix: "TW" },
  { code: "0050", name: "元大台灣50", suffix: "TW" },
  { code: "6488", name: "環球晶", suffix: "TWO" },
];

describe("filterStocks", () => {
  it("代號前綴", () => {
    expect(filterStocks(LIST, "23").map((s) => s.code)).toEqual(["2330", "2317"]);
  });
  it("中文名子字串", () => {
    expect(filterStocks(LIST, "台").map((s) => s.code)).toEqual(["2330", "0050"]);
  });
  it("空輸入回空", () => {
    expect(filterStocks(LIST, "")).toEqual([]);
  });
  it("取前 N(limit)", () => {
    expect(filterStocks(LIST, "", 0)).toEqual([]);
    expect(filterStocks(LIST, "2", 1).length).toBe(1);
  });
});
describe("tickerOf", () => {
  it("組 Yahoo ticker", () => {
    expect(tickerOf({ code: "2330", name: "台積電", suffix: "TW" })).toBe("2330.TW");
    expect(tickerOf({ code: "6488", name: "環球晶", suffix: "TWO" })).toBe("6488.TWO");
  });
});
```

- [ ] **Step 2: 跑 → 失敗**

- [ ] **Step 3: 實作** — `web/src/lib/autocomplete.ts`

```ts
export interface Stock { code: string; name: string; suffix: "TW" | "TWO"; }

/** 代號以 q 開頭 OR 中文名含 q;取前 limit 筆。空 q 回空。 */
export function filterStocks(list: Stock[], q: string, limit = 8): Stock[] {
  const t = q.trim();
  if (!t) return [];
  const out: Stock[] = [];
  for (const s of list) {
    if (s.code.startsWith(t) || s.name.includes(t)) {
      out.push(s);
      if (out.length >= limit) break;
    }
  }
  return out;
}

/** Stock → Yahoo ticker(2330.TW / 6488.TWO)。 */
export function tickerOf(s: Stock): string {
  return `${s.code}.${s.suffix}`;
}
```

- [ ] **Step 4: 跑 → 通過**

- [ ] **Step 5: Commit**
```bash
git add web/src/lib/autocomplete.ts web/test/autocomplete.test.ts
git commit -m "feat(web): stock autocomplete filter (TDD)"
```

---

### Task 6: web/src/lib/compare.ts(核心計算)

**Files:** Create `web/src/lib/compare.ts`、`web/test/compare.test.ts`

> 核心邏輯:給每檔的 adjclose 序列(已是各自交易日,但**起訖未對齊**),算共同區間、各檔投報率/最終金額/成長序列、vs 基準差額、贏家。

- [ ] **Step 1: 失敗測試**

```ts
import { describe, it, expect } from "vitest";
import { computeCompare, type SeriesInput } from "../src/lib/compare";

// 每檔:days=該股交易日(unix 秒,遞增)、adj=對應 adjclose。
const A: SeriesInput = { ticker: "2330.TW", name: "台積電",
  days: [10, 20, 30], adj: [100, 150, 200] };
const B: SeriesInput = { ticker: "0050.TW", name: "元大台灣50",
  days: [10, 20, 30], adj: [50, 55, 60] };

describe("computeCompare", () => {
  it("相同區間:投報率與最終金額", () => {
    const r = computeCompare([A, B], 100000, "0050.TW");
    const a = r.stocks.find((s) => s.ticker === "2330.TW")!;
    expect(a.roi).toBeCloseTo(1.0);          // 100→200 = +100%
    expect(a.final).toBeCloseTo(200000);
    const b = r.stocks.find((s) => s.ticker === "0050.TW")!;
    expect(b.roi).toBeCloseTo(0.2);          // 50→60 = +20%
    expect(b.final).toBeCloseTo(120000);
  });
  it("vs 基準差額($與%),基準為 0050", () => {
    const r = computeCompare([A, B], 100000, "0050.TW");
    const a = r.stocks.find((s) => s.ticker === "2330.TW")!;
    expect(a.diff).toBeCloseTo(80000);        // 200000 - 120000
    expect(a.diffPct).toBeCloseTo(80000 / 120000);
  });
  it("贏家 = 最終金額最高", () => {
    const r = computeCompare([A, B], 100000, "0050.TW");
    expect(r.winner).toBe("2330.TW");
  });
  it("成長序列首=本金、末=最終金額", () => {
    const r = computeCompare([A, B], 100000, "0050.TW");
    const a = r.stocks.find((s) => s.ticker === "2330.TW")!;
    expect(a.growth[0]).toBeCloseTo(100000);
    expect(a.growth[a.growth.length - 1]).toBeCloseTo(200000);
  });
  it("共同區間:某檔較晚才有資料時對齊", () => {
    const late: SeriesInput = { ticker: "X.TW", name: "晚", days: [20, 30], adj: [10, 12] };
    const r = computeCompare([A, late], 100000, "2330.TW");
    expect(r.adjusted).toBe(true);            // 區間被調整
    expect(r.from).toBe(20);                  // 共同起點 = max(10,20)
    expect(r.to).toBe(30);
    const a = r.stocks.find((s) => s.ticker === "2330.TW")!;
    expect(a.roi).toBeCloseTo(200 / 150 - 1); // 從 day20(150) 起算
  });
});
```

- [ ] **Step 2: 跑 → 失敗**

- [ ] **Step 3: 實作** — `web/src/lib/compare.ts`

```ts
export interface SeriesInput {
  ticker: string;
  name: string;
  days: number[]; // 交易日 unix 秒,遞增
  adj: number[];  // 對應 adjclose(已濾 null)
}
export interface StockResult {
  ticker: string;
  name: string;
  roi: number;
  final: number;
  diff: number;      // vs 基準金額差
  diffPct: number;   // vs 基準百分比
  growth: number[];  // 共同區間每個交易日的金額
}
export interface CompareResult {
  stocks: StockResult[];
  winner: string;       // ticker
  from: number;         // 共同起點 unix 秒
  to: number;           // 共同終點
  adjusted: boolean;    // 區間是否因對齊被調整
}

/** 取共同區間 [from,to]:from=各檔最早日的最大值,to=各檔最末日的最小值。 */
function commonRange(series: SeriesInput[]): { from: number; to: number } {
  const from = Math.max(...series.map((s) => s.days[0]));
  const to = Math.min(...series.map((s) => s.days[s.days.length - 1]));
  return { from, to };
}

/** 在某檔序列中,取 day >= from 的第一個 index 與 day <= to 的最後一個 index。 */
function clip(s: SeriesInput, from: number, to: number): { adj: number[]; days: number[] } {
  const adj: number[] = [];
  const days: number[] = [];
  for (let i = 0; i < s.days.length; i++) {
    if (s.days[i] >= from && s.days[i] <= to) { adj.push(s.adj[i]); days.push(s.days[i]); }
  }
  return { adj, days };
}

export function computeCompare(
  series: SeriesInput[],
  amount: number,
  baseTicker: string,
): CompareResult {
  const { from, to } = commonRange(series);
  // 是否被調整:有任一檔的原始起點晚於使用者區間最早、或終點早於最晚。
  const reqFrom = Math.min(...series.map((s) => s.days[0]));
  const reqTo = Math.max(...series.map((s) => s.days[s.days.length - 1]));
  const adjusted = from !== reqFrom || to !== reqTo;

  const clipped = series.map((s) => ({ s, c: clip(s, from, to) }));
  const interim = clipped.map(({ s, c }) => {
    const adjStart = c.adj[0];
    const adjEnd = c.adj[c.adj.length - 1];
    const roi = adjEnd / adjStart - 1;
    const final = amount * (adjEnd / adjStart);
    const growth = c.adj.map((v) => amount * (v / adjStart));
    return { ticker: s.ticker, name: s.name, roi, final, growth };
  });

  const base = interim.find((x) => x.ticker === baseTicker) ?? interim[0];
  const stocks: StockResult[] = interim.map((x) => ({
    ...x,
    diff: x.final - base.final,
    diffPct: base.final === 0 ? 0 : (x.final - base.final) / base.final,
  }));

  const winner = stocks.reduce((w, s) => (s.final > w.final ? s : w), stocks[0]).ticker;
  return { stocks, winner, from, to, adjusted };
}
```

- [ ] **Step 4: 跑 → 通過**

- [ ] **Step 5: Commit**
```bash
git add web/src/lib/compare.ts web/test/compare.test.ts
git commit -m "feat(web): core compare logic — roi/final/diff/winner/common-range (TDD)"
```

---

### Task 7: web/src/lib/chart.ts(SVG 多線成長圖)

**Files:** Create `web/src/lib/chart.ts`、`web/test/chart.test.ts`

> 純函式:把多檔 growth 序列轉成一段 `<svg>` 字串(viewBox 內的折線 + 圖例)。可單元測試輸出含預期 path/顏色。

- [ ] **Step 1: 失敗測試**

```ts
import { describe, it, expect } from "vitest";
import { lineChartSvg, COLORS } from "../src/lib/chart";

describe("lineChartSvg", () => {
  const data = [
    { name: "A", growth: [100000, 150000, 200000], color: COLORS[0] },
    { name: "B", growth: [100000, 110000, 120000], color: COLORS[1] },
  ];
  it("回 svg 字串含兩條 polyline", () => {
    const svg = lineChartSvg(data, 600, 300);
    expect(svg).toContain("<svg");
    expect((svg.match(/<polyline/g) ?? []).length).toBe(2);
    expect(svg).toContain(COLORS[0]);
    expect(svg).toContain(COLORS[1]);
  });
  it("空資料回空 svg(不報錯)", () => {
    expect(lineChartSvg([], 600, 300)).toContain("<svg");
  });
});
```

- [ ] **Step 2: 跑 → 失敗**

- [ ] **Step 3: 實作** — `web/src/lib/chart.ts`

```ts
export const COLORS = ["#2347d6", "#e8590c", "#2f9e44", "#9c36b5", "#1098ad"];

export interface ChartLine { name: string; growth: number[]; color: string; }

/** 多檔成長序列 → SVG 字串(折線 + 基準線)。所有序列長度相同(共同區間)。 */
export function lineChartSvg(lines: ChartLine[], w: number, h: number): string {
  const pad = { l: 8, r: 8, t: 10, b: 10 };
  const innerW = w - pad.l - pad.r;
  const innerH = h - pad.t - pad.b;
  if (lines.length === 0 || lines[0].growth.length === 0) {
    return `<svg viewBox="0 0 ${w} ${h}" xmlns="http://www.w3.org/2000/svg"></svg>`;
  }
  const n = lines[0].growth.length;
  const all = lines.flatMap((l) => l.growth);
  const min = Math.min(...all), max = Math.max(...all);
  const span = max - min || 1;
  const x = (i: number) => pad.l + (n === 1 ? 0 : (i / (n - 1)) * innerW);
  const y = (v: number) => pad.t + innerH - ((v - min) / span) * innerH;
  const polys = lines
    .map((l) => {
      const pts = l.growth.map((v, i) => `${x(i).toFixed(1)},${y(v).toFixed(1)}`).join(" ");
      return `<polyline fill="none" stroke="${l.color}" stroke-width="2" points="${pts}" />`;
    })
    .join("");
  return `<svg viewBox="0 0 ${w} ${h}" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="none">${polys}</svg>`;
}
```

- [ ] **Step 4: 跑 → 通過**

- [ ] **Step 5: Commit**
```bash
git add web/src/lib/chart.ts web/test/chart.test.ts
git commit -m "feat(web): SVG multi-line growth chart (TDD)"
```

---

### Task 8: web/src/lib/api.ts(呼叫 Worker)

**Files:** Create `web/src/lib/api.ts`

> 薄封裝:組 /api/compare 請求、回傳型別。無單元測試(純 IO 薄層;邏輯已在 compare.ts 測)。

- [ ] **Step 1: 實作**

```ts
import type { SeriesInput } from "./compare";

const API = import.meta.env.PUBLIC_API_URL ?? "https://stock-pk-api.chinte-cheng.workers.dev";

export interface CompareApiError { error: string; }

/** 打 Worker 取多檔 adjclose 序列。回 SeriesInput[] 或 throw。 */
export async function fetchCompare(
  tickers: string[],
  fromSec: number,
  toSec: number,
): Promise<SeriesInput[]> {
  const url = `${API}/api/compare?tickers=${encodeURIComponent(tickers.join(","))}&from=${fromSec}&to=${toSec}`;
  const res = await fetch(url, { signal: AbortSignal.timeout(12000) });
  if (!res.ok) {
    const body = (await res.json().catch(() => ({}))) as CompareApiError;
    throw new Error(body.error ?? `http_${res.status}`);
  }
  return (await res.json()) as SeriesInput[];
}

/** 訪客 beacon(fire-and-forget)。 */
export function beacon(): void {
  try {
    fetch(`${API}/api/visit`, { method: "GET", mode: "no-cors", keepalive: true }).catch(() => {});
  } catch { /* ignore */ }
}

/** 取訪客數(失敗回 null)。 */
export async function fetchUses(): Promise<number | null> {
  try {
    const res = await fetch(`${API}/api/stats`, { signal: AbortSignal.timeout(8000) });
    if (!res.ok) return null;
    const d = (await res.json()) as { views?: number };
    return typeof d.views === "number" ? d.views : null;
  } catch { return null; }
}
```

- [ ] **Step 2: typecheck** — Run: `cd ~/projects/stock-pk && npx tsc -p web/tsconfig.json` → 0 錯（注意:`import.meta.env` 需 astro 型別,astro tsconfig 已含;若報錯加 `/// <reference types="astro/client" />` 到 env.d.ts）

- [ ] **Step 3: Commit**
```bash
git add web/src/lib/api.ts
git commit -m "feat(web): worker api client (compare + visitor)"
```

---

### Task 9: web UI — Base.astro + index.astro + StockSlots + Results(整合)

**Files:** Create `web/src/layouts/Base.astro`、`web/src/pages/index.astro`、`web/src/components/StockSlots.astro`、`web/src/components/Results.astro`

> 此為整合任務:把 lib 組裝成可互動單頁。以 `astro build` 成功 + 關鍵元素存在為驗收(Astro 元件無單元測試框架)。

- [ ] **Step 1: Base.astro**(亮色技術風 + 「Derek Chen」字標 + 履歷連結 + 訪客 beacon)

```astro
---
const apiBeacon = true;
---
<!doctype html>
<html lang="zh-Hant">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>台股 PK · Derek Chen</title>
    <link href="https://fonts.googleapis.com/css2?family=Hanken+Grotesk:wght@400;600;800&family=Space+Mono&family=Noto+Sans+TC:wght@400;500;700&display=swap" rel="stylesheet" />
    <style is:global>
      :root{--bg:#faf9f6;--bg-elev:#fff;--ink:#18181b;--ink-soft:#3f3f46;--muted:#6b7280;--line:#e7e4dd;--accent:#2347d6;--accent-soft:#eef1fd;--sans:"Hanken Grotesk","Noto Sans TC",system-ui,sans-serif;--mono:"Space Mono","Noto Sans TC",monospace;}
      *{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font-family:var(--sans);line-height:1.6}
      a{color:var(--accent);text-decoration:none}.wrap{max-width:860px;margin:0 auto;padding:0 1.2rem}
      .head{border-bottom:1px solid var(--line);background:var(--bg)}
      .head .row{display:flex;align-items:center;gap:1rem;height:56px}
      .wordmark{font-weight:800;color:var(--ink)}.wordmark .t{color:var(--accent)}
      .head nav{margin-left:auto;font-size:.88rem}
      .foot{border-top:1px solid var(--line);margin-top:4rem;padding:2rem 0;color:var(--muted);font-family:var(--mono);font-size:.76rem}
    </style>
  </head>
  <body>
    <header class="head"><div class="wrap row">
      <a class="wordmark" href="https://derek-chen.pages.dev">Derek Chen<span class="t">.</span></a>
      <nav><a href="https://derek-chen.pages.dev">← 回作品集</a></nav>
    </div></header>
    <main class="wrap"><slot /></main>
    <footer class="foot"><div class="wrap">
      © 2026 Derek Chen · 台股多檔 PK · <a href="https://derek-chen.pages.dev">作品集</a>
      · <span id="uses">—</span> 人用過
    </div></footer>
    <script>
      import { beacon, fetchUses } from "../lib/api";
      beacon();
      fetchUses().then((n) => { if (n != null) document.getElementById("uses")!.textContent = String(n); });
    </script>
  </body>
</html>
```

- [ ] **Step 2: StockSlots.astro**(2~5 槽 + 候選 + 新增/移除;client script 用 autocomplete.ts)

實作要點(完整 client 邏輯):
- 容器 `#slots`,初始 2 槽(槽 1 空、槽 2 預設 0050)。每槽:`<input class="stk" data-ticker="">` + 候選 `<ul class="cands">` + 移除鈕(≥3 槽時可移除,保留最少 2)。
- 「+ 新增比較股票」鈕 `#add`(到 5 檔 disable)。
- client script:`fetch("/stocks.json")` 載清單;input 事件 → `filterStocks` 顯示候選;點候選 → 填 `code name`、設 `input.dataset.ticker = tickerOf(s)`。
- 對外:提供 `window.__getTickers()` 回所有已選 ticker(供 index.astro 計算用),或用自訂事件。**用 module 變數 + export 函式**:把 slots 邏輯寫成 `web/src/lib/slots-ui.ts`,index 的 script import 它取 `getSelectedTickers()`。

> 為可測 + 乾淨,把 DOM 無關的「槽位狀態管理」最小化;選股驗證(必須從候選選、不可亂打)由 `dataset.ticker` 是否有值判斷。

- [ ] **Step 3: Results.astro**(結果卡片容器 + 圖容器,空殼;由 client 動態填)

```astro
<section id="results" hidden>
  <div id="cards" class="cards"></div>
  <div id="chart" class="chart"></div>
  <p id="note" class="note"></p>
</section>
```

- [ ] **Step 4: index.astro**(組裝 + 計算流程)

要點:
- 用 Base 包;放輸入區(StockSlots、日期快捷鈕 PRESETS.map、自訂起訖 `<input type=date>`、金額 `<input type=number value=100000>`、計算鈕)+ Results。
- client script:
  - 日期快捷鈕點擊 → `presetRange(key, Date.now())` 設兩個 date input。
  - 計算鈕 → 收集 tickers(≥2,皆已選)、from/to(date input → unix 秒,`clampEnd`、`validRange`)、amount → `fetchCompare` → `computeCompare(series, amount, baseTicker)` → 渲染卡片(每檔 roi%/final/diff,贏家加 👑)+ `lineChartSvg` 注入 `#chart` + 若 `adjusted` 顯示區間調整提示。
  - 錯誤(驗證失敗 / api throw)→ 顯示友善訊息。
  - 基準預設第一個 0050(若有),可在卡片上切換基準 → 重算 diff(不需重打 API,重跑 computeCompare)。
- XSS:所有數字/股名用 `textContent`;SVG 用 `innerHTML` 僅限我們自產的 chart 字串(無使用者輸入注入,股名不進 SVG)。

- [ ] **Step 5: build 驗證**

Run: `cd web && npm run build`
Expected: build 成功;`dist/index.html` 存在且含 `id="slots"`、`id="results"`、`id="chart"`、計算鈕。
（需先有 `web/public/stocks.json` — Task 2 已產;若無先跑 `npm run build:stocks`。）

- [ ] **Step 6: Commit**
```bash
git add web/src
git commit -m "feat(web): PK tool UI — slots, presets, results cards, growth chart"
```

---

### Task 10: api workspace 骨架 + visits(複用 devbox 模式)

**Files:** Create `api/package.json`、`api/tsconfig.json`、`api/wrangler.toml`、`api/schema.sql`、`api/vitest.config.ts`、`api/src/visits.ts`、`api/test/{env.d.ts,visits.test.ts}`

- [ ] **Step 1: api 設定檔**

`api/package.json`:
```json
{ "name": "@stock-pk/api", "type": "module", "private": true,
  "scripts": { "test": "vitest run", "deploy": "wrangler deploy" },
  "devDependencies": { "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@cloudflare/workers-types": "^4.20250109.0", "vitest": "~3.2.0", "wrangler": "^4.0.0" } }
```
`api/tsconfig.json`:
```json
{ "extends": "../tsconfig.base.json",
  "compilerOptions": { "types": ["@cloudflare/workers-types"] },
  "include": ["src/**/*.ts"] }
```
`api/wrangler.toml`:
```toml
name = "stock-pk-api"
main = "src/index.ts"
compatibility_date = "2025-09-06"

[[d1_databases]]
binding = "DB"
database_name = "stockpk"
database_id = "PLACEHOLDER_FILL_AFTER_d1_create"

# secret: wrangler secret put VISIT_SALT
```
`api/schema.sql`:
```sql
CREATE TABLE IF NOT EXISTS visits (
  ts INTEGER NOT NULL, day TEXT NOT NULL, ip_hash TEXT NOT NULL, country TEXT, path TEXT
);
CREATE INDEX IF NOT EXISTS idx_visits_day ON visits(day);
```
`api/vitest.config.ts`:
```ts
import { defineWorkersConfig } from "@cloudflare/vitest-pool-workers/config";
export default defineWorkersConfig({
  test: { poolOptions: { workers: {
    isolatedStorage: true,
    wrangler: { configPath: "./wrangler.toml" },
    miniflare: { bindings: { VISIT_SALT: "test-salt" } },
  } } },
});
```
`api/test/env.d.ts`:
```ts
/// <reference types="@cloudflare/vitest-pool-workers" />
import type { Env } from "../src/index";
declare module "cloudflare:test" {
  // eslint-disable-next-line @typescript-eslint/no-empty-object-type
  interface ProvidedEnv extends Env {}
}
```

- [ ] **Step 2: visits.ts**(直接採用 devbox 已驗證版本)

`api/src/visits.ts`:
```ts
export function utcDay(ts: number): string { return new Date(ts).toISOString().slice(0, 10); }

export async function hashIp(ip: string, salt: string, day: string): Promise<string> {
  const data = new TextEncoder().encode(`${ip}|${salt}|${day}`);
  const buf = await crypto.subtle.digest("SHA-256", data);
  return [...new Uint8Array(buf)].map((b) => b.toString(16).padStart(2, "0")).join("");
}
export interface VisitEnv { DB: D1Database; VISIT_SALT?: string; }
export async function recordVisit(env: VisitEnv, ip: string, country: string, path: string): Promise<void> {
  const ts = Date.now(), day = utcDay(ts);
  const ipHash = await hashIp(ip, env.VISIT_SALT ?? "", day);
  await env.DB.prepare("INSERT INTO visits (ts, day, ip_hash, country, path) VALUES (?,?,?,?,?)")
    .bind(ts, day, ipHash, country || null, path || null).run();
}
export interface VisitStats { views: number; uniqueToday: number; }
export async function fetchVisitStats(env: VisitEnv): Promise<VisitStats | null> {
  try {
    const today = utcDay(Date.now());
    const [v, u] = await Promise.all([
      env.DB.prepare("SELECT count(*) AS n FROM visits").first<{ n: number }>(),
      env.DB.prepare("SELECT count(DISTINCT ip_hash) AS n FROM visits WHERE day = ?").bind(today).first<{ n: number }>(),
    ]);
    return { views: v?.n ?? 0, uniqueToday: u?.n ?? 0 };
  } catch (e) { console.error("fetchVisitStats failed:", e); return null; }
}
```

- [ ] **Step 3: visits.test.ts**(複用 devbox 測試:utcDay/hashIp 純函式 + recordVisit/fetchVisitStats D1,beforeAll 建表、beforeEach 清表;同 ip×2→views=2/uniqueToday=1)。寫出完整測試(參考 devbox `api/test/visits.test.ts` 的等價內容)。

- [ ] **Step 4: 裝依賴 + 測試**

Run: `cd ~/projects/stock-pk && npm install && cd api && npx vitest run visits`
Expected: visits 測試綠。

- [ ] **Step 5: Commit**
```bash
git add api package-lock.json
git commit -m "chore(api): worker scaffold + visits (D1, reused pattern)"
```

---

### Task 11: api/src/yahoo.ts(代理解析,TDD)

**Files:** Create `api/src/yahoo.ts`、`api/test/yahoo.test.ts`

- [ ] **Step 1: 失敗測試**

```ts
import { describe, it, expect } from "vitest";
import { yahooUrl, parseChart } from "../src/yahoo";

describe("yahooUrl", () => {
  it("組正確 URL(period1/period2/interval)", () => {
    const u = yahooUrl("2330.TW", 1000, 2000);
    expect(u).toContain("/v8/finance/chart/2330.TW");
    expect(u).toContain("period1=1000");
    expect(u).toContain("period2=2000");
    expect(u).toContain("interval=1d");
  });
});
describe("parseChart", () => {
  const sample = { chart: { result: [{
    meta: { shortName: "TSMC" },
    timestamp: [10, 20, 30],
    indicators: { adjclose: [{ adjclose: [100, null, 200] }] },
  }], error: null } };
  it("取 days/adj 並濾掉 null", () => {
    const r = parseChart(sample, "2330.TW");
    expect(r).not.toBeNull();
    expect(r!.days).toEqual([10, 30]);
    expect(r!.adj).toEqual([100, 200]);
    expect(r!.name).toBe("TSMC");
  });
  it("空結果回 null", () => {
    expect(parseChart({ chart: { result: null } }, "X")).toBeNull();
  });
});
```

- [ ] **Step 2: 跑 → 失敗**

- [ ] **Step 3: 實作** — `api/src/yahoo.ts`

```ts
export interface ParsedChart { ticker: string; name: string; days: number[]; adj: number[]; }

export function yahooUrl(ticker: string, period1: number, period2: number): string {
  return `https://query1.finance.yahoo.com/v8/finance/chart/${encodeURIComponent(ticker)}?period1=${period1}&period2=${period2}&interval=1d`;
}

/** 解析 Yahoo chart 回應,取 timestamp + adjclose(濾 null 對)。空/壞回 null。 */
export function parseChart(json: unknown, ticker: string): ParsedChart | null {
  const result = (json as { chart?: { result?: unknown[] } })?.chart?.result;
  if (!Array.isArray(result) || result.length === 0) return null;
  const r = result[0] as {
    meta?: { shortName?: string; longName?: string };
    timestamp?: number[];
    indicators?: { adjclose?: Array<{ adjclose?: (number | null)[] }> };
  };
  const ts = r.timestamp ?? [];
  const adjArr = r.indicators?.adjclose?.[0]?.adjclose ?? [];
  const days: number[] = [];
  const adj: number[] = [];
  for (let i = 0; i < ts.length; i++) {
    const v = adjArr[i];
    if (typeof v === "number" && Number.isFinite(v)) { days.push(ts[i]); adj.push(v); }
  }
  if (days.length === 0) return null;
  return { ticker, name: r.meta?.shortName ?? r.meta?.longName ?? ticker, days, adj };
}

/** 抓單檔(逾時 6s)。失敗回 null。 */
export async function fetchChart(ticker: string, p1: number, p2: number): Promise<ParsedChart | null> {
  try {
    const res = await fetch(yahooUrl(ticker, p1, p2), {
      headers: { "user-agent": "Mozilla/5.0" },
      signal: AbortSignal.timeout(6000),
    });
    if (!res.ok) return null;
    return parseChart(await res.json(), ticker);
  } catch { return null; }
}
```

- [ ] **Step 4: 跑 → 通過**

- [ ] **Step 5: Commit**
```bash
git add api/src/yahoo.ts api/test/yahoo.test.ts
git commit -m "feat(api): yahoo chart url + parse (TDD)"
```

---

### Task 12: api/src/index.ts(路由:/api/compare + /api/visit + /api/stats)

**Files:** Create `api/src/index.ts`、`api/test/routes.test.ts`

- [ ] **Step 1: 失敗測試** — `api/test/routes.test.ts`

```ts
import { env, SELF } from "cloudflare:test";
import { describe, it, expect, beforeAll } from "vitest";

beforeAll(async () => {
  await env.DB.exec("CREATE TABLE IF NOT EXISTS visits (ts INTEGER NOT NULL, day TEXT NOT NULL, ip_hash TEXT NOT NULL, country TEXT, path TEXT)");
});

describe("guardrails", () => {
  it("/api/visit 回 204 + CORS", async () => {
    const r = await SELF.fetch("https://x/api/visit");
    expect(r.status).toBe(204);
    expect(r.headers.get("access-control-allow-origin")).toBe("*");
  });
  it("/api/stats 回 views/uniqueToday", async () => {
    await SELF.fetch("https://x/api/visit");
    const r = await SELF.fetch("https://x/api/stats");
    const b = await r.json<{ views: number }>();
    expect(typeof b.views).toBe("number");
  });
  it("/api/compare 缺 tickers 回 400", async () => {
    const r = await SELF.fetch("https://x/api/compare?from=1&to=2");
    expect(r.status).toBe(400);
  });
  it("/api/compare 超過 5 檔回 400", async () => {
    const r = await SELF.fetch("https://x/api/compare?tickers=a,b,c,d,e,f&from=1&to=2");
    expect(r.status).toBe(400);
  });
});
```
> 注:happy-path(真的打 Yahoo)**不寫自動測試**(避免測試打外部 API);留人工驗收。

- [ ] **Step 2: 跑 → 失敗**

- [ ] **Step 3: 實作** — `api/src/index.ts`

```ts
import { recordVisit, fetchVisitStats } from "./visits";
import { fetchChart } from "./yahoo";

export interface Env { DB: D1Database; VISIT_SALT?: string; }

const CORS = { "access-control-allow-origin": "*" };
const MAX_TICKERS = 5;

export default {
  async fetch(req: Request, env: Env): Promise<Response> {
    const url = new URL(req.url);
    const path = url.pathname;

    if (req.method === "GET" && path === "/api/visit") {
      try {
        const ip = req.headers.get("cf-connecting-ip") ?? "0.0.0.0";
        const country = (req as unknown as { cf?: { country?: string } }).cf?.country ?? "";
        await recordVisit(env, ip, country, "/");
      } catch { /* 不報錯 */ }
      return new Response(null, { status: 204, headers: CORS });
    }

    if (req.method === "GET" && path === "/api/stats") {
      const s = await fetchVisitStats(env);
      return Response.json({ views: s?.views ?? 0, uniqueToday: s?.uniqueToday ?? 0 }, { headers: CORS });
    }

    if (req.method === "GET" && path === "/api/compare") {
      const raw = (url.searchParams.get("tickers") ?? "").split(",").map((t) => t.trim()).filter(Boolean);
      const from = Number(url.searchParams.get("from"));
      const to = Number(url.searchParams.get("to"));
      if (raw.length < 1 || raw.length > MAX_TICKERS || !Number.isFinite(from) || !Number.isFinite(to) || from >= to) {
        return Response.json({ error: "bad_request" }, { status: 400, headers: CORS });
      }
      // 簡單格式檢查:^[0-9A-Za-z]{2,6}\.(TW|TWO)$
      if (!raw.every((t) => /^[0-9A-Za-z]{2,6}\.(TW|TWO)$/.test(t))) {
        return Response.json({ error: "bad_ticker" }, { status: 400, headers: CORS });
      }
      const cache = caches.default;
      const results = await Promise.all(
        raw.map(async (ticker) => {
          const key = new Request(`https://cache/${ticker}/${from}/${to}`);
          const hit = await cache.match(key);
          if (hit) return hit.json();
          const parsed = await fetchChart(ticker, from, to);
          if (parsed) {
            const resp = Response.json(parsed, { headers: { "cache-control": "max-age=86400" } });
            await cache.put(key, resp.clone());
            return parsed;
          }
          return null;
        }),
      );
      if (results.some((r) => r === null)) {
        return Response.json({ error: "no_data" }, { status: 502, headers: CORS });
      }
      return Response.json(results, { headers: CORS });
    }

    return new Response("stock-pk api", { status: 200, headers: CORS });
  },
};
```

- [ ] **Step 4: 跑 → 通過** — Run: `cd api && npx vitest run` → 全綠;`npm run typecheck` 0 錯。

- [ ] **Step 5: Commit**
```bash
git add api/src/index.ts api/test/routes.test.ts
git commit -m "feat(api): /api/compare (yahoo proxy + cache) + visit routes"
```

---

### Task 13: CI + 部署設定(controller 在 code 完成後執行)

**Files:** Create `.github/workflows/ci.yml`

> 此 Task 含基礎設施步驟,由 controller 在所有 code + review 後做(建 GitHub repo / Cloudflare 資源 / secrets / 部署)。

- [ ] **Step 1: ci.yml**

```yaml
name: ci
on:
  push: { branches: [main] }
  pull_request:
jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 24, cache: npm }
      - run: npm ci
      - run: npm run build:stocks
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test
        env: { CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }} }
  deploy:
    needs: quality
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 24, cache: npm }
      - run: npm ci
      - run: npm run build:stocks
      - name: Deploy worker
        run: npx wrangler deploy --config api/wrangler.toml
        env: { CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }} }
      - name: Build & deploy pages
        run: |
          cd web && PUBLIC_API_URL=https://stock-pk-api.chinte-cheng.workers.dev npm run build
          npx wrangler pages deploy dist --project-name derek-stock-pk --branch main
        env: { CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }} }
```

- [ ] **Step 2: Cloudflare 資源(controller 手動)**
```bash
cd ~/projects/stock-pk/api
npx wrangler d1 create stockpk          # 把 database_id 填回 wrangler.toml
npx wrangler d1 execute stockpk --remote --file schema.sql
openssl rand -hex 32 | npx wrangler secret put VISIT_SALT
```

- [ ] **Step 3: GitHub repo + secret(controller)**
```bash
cd ~/projects/stock-pk
gh repo create stock-pk --public --source=. --remote=origin   # 作品集 → public(見 spec)
gh secret set CLOUDFLARE_API_TOKEN < ~/.cf_ci_token
git push -u origin main
```

- [ ] **Step 4: 首次部署 + 驗收**:Worker + Pages 部署;實測比 2330 vs 0050 1年、加到 5 檔、未上市區間提示、訪客數遞增。

- [ ] **Step 5: 履歷站加連結**(devbox repo,獨立小改):在 devbox 專案區或 nav 加一個連到 `derek-stock-pk.pages.dev` 的超連結。

---

## 注意事項

- **web 純函式測試用 node 環境**(vitest.config environment: node);**api 用 workers pool**。兩 workspace 各自 vitest。
- **Yahoo happy-path 不寫自動測試**(避免測試打外部 API + 不穩定);parseChart 用範例 JSON 測解析,fetchChart 留人工。
- **快取**:`caches.default` 對過去日資料快取 1 天;`to` 含今天時當天資料會變,但 1 天 TTL 可接受(或實作時對「to≥今天」不快取——先簡單做)。
- **DRY**:visits.ts 直接採用 devbox 已驗證版本(同檔內容),不重新發明。
- **基準切換**:純前端 `computeCompare` 重算,不重打 API。
- **YAGNI**:不做定期定額、>5 檔、進階指標、美股。
- **stocks.json**:`.gitignore` 忽略,CI build 時 `npm run build:stocks` 產生;本機開發前先手動跑一次。
