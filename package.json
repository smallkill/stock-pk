{
  "name": "stock-pk",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "workspaces": ["api", "web"],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc -p api/tsconfig.json && tsc -p web/tsconfig.json",
    "test": "npm run test --workspaces --if-present",
    "build:stocks": "node scripts/build-stocks.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "eslint": "^9.9.0",
    "typescript-eslint": "^8.2.0"
  }
}
